This app has been created in React using Create React App. It allows you to see the data
from api (punkapi.com), using axios Application display beers: name, image, tagline.
User can click a beer name to see the details of the beer ( or by manually entering the page using it’s URL address
  (/detail/:id)) and the beer to section Favourite. The Detail page also list up to 3 similar beers as suggestion.



If you want to run this app you have to use the following instructions in your console:

1.  

 npm i


2. Additionally you will have to run:

npm i axios

npm i react-alert


3. To start project run:

npm start

4. The project will be available at: localhost:3000,
